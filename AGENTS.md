# AGENTS.md

本文件定義 Idle-RPG 專案中各 AI 的角色、責任範圍與協作方式。

所有 AI 開始工作前，必須先閱讀：

- `AI_RULES.md`
- `docs/AI_WORKFLOW.md`
- `docs/AI_TASKS.md`

若本文件與其他協議發生衝突，優先順序為：

1. 使用者當前明確指示
2. `AI_RULES.md`
3. `docs/AI_WORKFLOW.md`
4. `AGENTS.md`
5. `docs/AI_TASKS.md`

---

# 1. 最高原則

多 AI 協作的目的是降低整體開發時間，而不是增加參與的 AI 數量。

不得為了使用多 AI 而刻意拆分任務。

如果拆分、提示詞傳遞、分支同步、Merge 或衝突處理的成本，高於由單一 AI 直接完成的成本，應改採單一主開發者模式。

預設由主開發者完整處理：

- 需求分析
- 架構判斷
- 程式實作
- 測試
- 文件更新
- Commit
- 完成交接

只有在任務適合安全並行時，才交由其他 AI 執行。

---

# 2. 主協調者

Claude Code 預設擔任主協調者與主開發者。

Claude Code 負責：

- 理解使用者需求
- 分析現有架構
- 判斷任務規模與風險
- 決定由自己完成或分派其他 AI
- 拆分任務與標示前置依賴
- 維護 `docs/AI_TASKS.md`
- 完成主要程式修改
- 統整其他 AI 的工作成果
- 執行或安排 Code Review
- 回報目前狀態與下一步

Claude Code 不得因為另外兩個 AI 存在，就固定產生 Codex 或 Antigravity 的任務。

如果工作能由 Claude Code 連續完成，且拆分不會明顯節省時間，應直接完成，不需分派。

---

# 3. 任務分派條件

只有在下列條件全部成立時，才可以分派給其他 AI：

- 任務可獨立完成。
- 任務的輸入與輸出明確。
- 不依賴尚未完成的設計決策。
- 不修改相同檔案。
- 不會同時修改同一個 API、資料格式、共用契約或狀態模型。
- 可以獨立測試。
- 可以建立獨立 Commit。
- 平行執行可明顯降低整體完成時間。

如果任一條件不成立，應由主開發者繼續完成。

不得將高度共享上下文的工作拆給不同 AI，例如：

- 同一功能的程式、測試與文件同步
- 同一套 Protocol 的定義與驗證
- 同一資料格式的讀取與存檔
- 同一 UI 狀態系統的多處修改
- 需要頻繁互相確認結果的連續工作

---

# 4. Codex 角色

Codex 預設為按需啟用的程式實作與分析專家。**由使用者決定何時啟用**——
主協調者不得為了「這支檔案慣例上屬於 Codex」而主動把工作轉出去或產生提示詞
（`AI_RULES.md` 第 3.1 節）。

適合交給 Codex 的工作：

- 獨立程式功能實作
- 測試案例補強
- 演算法分析
- 效能分析
- 大量但規則明確的程式修改
- 獨立工具腳本
- 靜態程式碼檢查
- 特定模組重構

不適合交給 Codex 的工作：

- 尚未完成架構決策的功能
- 需要頻繁修改 Claude 正在處理之檔案的工作
- 單純為了讓 Codex 有工作而拆出的任務
- 無法獨立驗證的零碎修改

Codex 接到任務後，只能處理提示詞與 `docs/AI_TASKS.md` 明確授權的範圍。

發現任務範圍不完整、依賴未滿足或需要修改禁止檔案時，必須停止並回報，不得自行擴大範圍。

---

# 5. Antigravity 角色

Antigravity 預設為按需啟用的操作驗證與 UI 專家。

適合交給 Antigravity 的工作：

- 瀏覽器實際操作
- UI 功能驗證
- 畫面流程驗證
- Bug 重現
- Console 錯誤檢查
- Regression Test
- 唯讀盤點
- UI 狀態與互動流程分析
- 獨立且明確的前端修改

不適合交給 Antigravity 的工作：

- 與主開發者同時修改相同 UI 檔案
- 尚未確定規格的功能
- 只需要閱讀程式碼即可完成的簡單確認
- 無法重現或沒有驗收條件的模糊任務

Antigravity 執行驗證任務時，預設採唯讀模式。

只有任務明確授權修改檔案時，才可以進行程式修改。

---

# 6. Reviewer 角色

Code Review 預設採唯讀模式。

Reviewer 應檢查：

- 邏輯錯誤
- 架構一致性
- 任務外修改
- API 與資料格式相容性
- 存檔相容性
- Timer、Event 與非同步問題
- 效能與記憶體問題
- 測試是否足夠
- 文件是否同步
- 是否存在未處理風險

Reviewer 預設只輸出問題清單，不直接修改程式碼。

Review 回報格式：

- 嚴重程度
- 問題位置
- 問題原因
- 可能影響
- 建議修正方式

嚴重程度：

- Critical
- High
- Medium
- Low
- Suggestion

如果需要 Reviewer 直接修正，必須建立獨立任務並明確授權修改範圍。

---

# 7. 任務執行模式

依任務情況，採用以下三種模式。

## 模式 A：單一主開發者

適用於：

- 一般功能
- Bug 修復
- 小型重構
- 測試與文件同步
- 需要共享大量上下文的工作

流程：

1. Claude Code 分析
2. Claude Code 實作
3. Claude Code 測試
4. Claude Code 更新文件
5. Claude Code 建立 Commit
6. 必要時交由其他 AI 唯讀 Review

這是預設模式。

## 模式 B：主開發者加 Reviewer

適用於：

- 存檔格式變更
- Worker Protocol 變更
- 核心公式變更
- 大型重構
- 多模組狀態同步
- 高風險功能

流程：

1. 主開發者完成實作
2. Reviewer 唯讀檢查
3. 主開發者修正
4. 必要時進行實際操作驗證
5. 使用者合併

## 模式 C：平行開發

只有任務符合安全並行條件時使用。

流程：

1. Claude Code 拆分獨立任務
2. 記錄 Owner、依賴、修改範圍與驗收方式
3. 各 AI 在自己的 Worktree 執行
4. 每項任務完成後獨立 Review
5. 不需等待無關任務全部完成
6. 依依賴順序分別合併至 `develop`

不得預設每一輪都使用模式 C。

---

# 8. 下一步判斷

AI 完成目前任務後，不以 P0、P1、P2 編號決定是否繼續。

應依序判斷：

1. 下一項任務的前置依賴是否完成？
2. 是否會與其他 AI 的修改範圍衝突？
3. 是否需要等待使用者裁決？
4. 是否可以由目前 AI 直接完成？
5. 是否值得分派其他 AI？

如果依賴已完成且沒有衝突，可以立即繼續。

如果依賴尚未完成，應將任務標示為 `Blocked`。

不得等待與目前任務無關的其他 AI 工作完成。

---

# 9. 提示詞產生原則

主協調者只有在確定要分派任務時，才產生其他 AI 的提示詞。

提示詞至少包含：

- 任務名稱
- 任務目的
- 優先級
- 前置依賴
- 允許修改的檔案
- 禁止修改的檔案
- 具體修改內容
- 驗證方式
- Commit 要求
- 回報格式

不得一次提供大量尚未確定會執行的備用工作。

不得把彼此高度相依的多個任務包成長提示詞，再要求其他 AI 自行判斷順序。

若多項工作適合由同一 AI 連續完成，應合併為一個完整任務，避免重複理解上下文。

---

# 10. 任務交付格式

每個 AI 完成任務後，必須回報：

- 任務編號
- 完成內容
- 修改檔案
- 未修改但檢查過的檔案
- Commit 編號
- 執行的測試指令
- 測試結果
- 已知風險
- 未完成項目
- 建議下一步
- 是否可以合併

不得只回覆「已完成」。

---

# 11. 禁止事項

所有 AI 均不得：

- 為了使用多 AI 而強行拆分任務
- 未經授權修改**進行中任務**所佔用的檔案
  （使用者的指派本身即為授權；檔案歸屬慣例不是門檻，見 `AI_RULES.md` 第 3.1 節）
- 在依賴未完成時自行假設結果
- 把功能缺失改成測試接受現況
- 為了讓測試通過而降低正確性
- 修改最高權威資料以配合程式錯誤
- 未經使用者允許自行合併至 `develop`
- 未經使用者允許自行推送 `develop`
- 未經使用者允許發布至 `main`
- 在工作區不乾淨時同步或切換分支
- 省略必要測試或風險回報