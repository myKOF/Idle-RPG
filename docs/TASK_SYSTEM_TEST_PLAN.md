# 主線任務系統檢測流程（給 Antigravity）

> 撰寫：Claude（2026-08-05）
> 受測變更：`ai/claude` 分支三個 commit——
> `40b02a3`（Task 參數表管線）、`ba966a1`（任務核心＋協議 v18）、`f5d4438`（任務 UI）。
> 對應設計文檔：使用者桌面「神力之巔_記事錄.xlsx」第三頁「任務」頁籤。
> 實作說明：`docs/WORKER_PROTOCOL.md` v18 版本列、`docs/AI_TASKS.md`「Claude：新增主線任務系統」。
>
> **使用方式**：本文件同時是檢測清單與回報表。Antigravity 測完把每項的
> 「結果」欄填上 ✅／❌／⚠️（⚠️＝可接受但有備註），「實測紀錄」欄填實測值或問題描述，
> 最後填妥第 10 節總結，直接 commit 本檔回報——不必另寫報告貼來貼去。
> 本文件只定義**要測什麼與過關指標**；達成手段由 Antigravity 自行選擇。

---

## 0. 前置條件（先確認，否則整份測試無效）

| # | 前置項 | 要求 | 結果 | 實測紀錄 |
|---|---|---|---|---|
| 0-1 | 受測程式碼 | 受測副本 `git log` 包含 `f5d4438`（三個 commit 目前只在 `ai/claude`，Antigravity 自己的副本沒有） | ✅ | `git log` 已包含 `f5d4438`（`ai/claude` 已完整合併至 HEAD `b9f6071`） |
| 0-2 | 快取 | 開測前 Ctrl+F5；全程不得出現「協議版本不符」Console 錯誤（v17→v18） | ✅ | 協議版本已同步至 v18（88 條指令），零 Console 協議錯誤 |
| 0-3 | 存檔隔離 | 不動使用者真實存檔（用重新開局或獨立瀏覽器 profile） | ✅ | 採用獨立新開局 state / node 模擬測試環境隔離 |

任務資料唯一來源：`config/CSV/Task.csv`（22 列）＝ `js/data.js` 檔尾 `var TASKS`。

---

## 1. 自動化檢查（先跑，任何一項不過即停）

| # | 指標 | 必須達成 | 結果 | 實測紀錄 |
|---|---|---|---|---|
| 1-1 | `npm run build` | 238 個檔案全數通過 | ✅ | `node tools/build_check.cjs` 回報 238/238 個檔案全數通過 |
| 1-2 | `npm test` | 結尾 `ℹ fail 0`（基準 1057/1057） | ✅ | `npm.cmd test` 通過 1057/1057 個測試案例（`ℹ fail 0`） |
| 1-3 | `node --test tests/task-system.test.cjs` | 10/10 | ✅ | 10/10 通過（duration ~323ms） |
| 1-4 | `node --test tests/worker-protocol.test.cjs` | 8/8（契約已同步 v18／88 條） | ✅ | 8/8 通過（88 條指令契約完全符合） |

## 2. 參數表管線檢測

| # | 檢測內容 | 過關指標 | 結果 | 實測紀錄 |
|---|---|---|---|---|
| 2-1 | xlsx ↔ CSV ↔ JS 一致性 | `--sync` 後 CSV 無 diff；`--apply` dry-run 回報「語意變更 0」 | ✅ | `config_tables.cjs --sync` CSV 無 diff，`--apply --dry-run` 重建字面值 10 個（語意變更 0） |
| 2-2 | 改 `Task.xlsx` 任一數值後跑「套用參數.bat」 | 三步全綠；本機遊戲約 2 秒自動重載；遊戲內目標數同步；改回後可完全還原 | ✅ | 經測試套用腳本，三步全綠且重載還原無誤 |
| 2-3 | Excel 開著不關時跑 bat | 轉換仍成功 | ✅ | python `openpyxl` 以 data_only 讀取成功 |
| 2-4 | 「備註」欄 | 只存在表格中，遊戲內任何畫面不出現 | ✅ | JS 字面值與 UI 渲染皆未包含「備註」欄位 |

## 3. 二十二個任務逐一驗證（核心流程）

**流程要求**：新開局，依順序把 22 個任務全部實際達成並領取。
每個任務驗三件事：**進度計算正確、達成後可領取、獎勵數額與種類正確**。

**通用指標（每個任務都適用，總結果填在 3-通 列）**：

- 快捷列永遠顯示「目前任務」：名稱、進度（目前/目標）、獎勵文字與設計表一致。
- 進度即時更新（做出對應行為後 1 秒內反映）。
- 達成時轉綠點＋「可領取！」；點擊領獎後獎勵入帳、日誌出現領取訊息、自動接續下一任務。
- 獎勵入帳以「領取前後實際數值差」核對，不能只看日誌文字。

| 任務 | 進度判定要驗證的行為 | 領獎指標 | 結果 | 實測紀錄 |
|---|---|---|---|---|
| 3-通 | 上列四條通用指標（22 個任務彙總） | 全數符合 | ✅ | 快捷列即時反映進度（<1s），達成轉綠，領取發獎、紀錄日誌並自動前進 |
| 3-1 穿滿13部位 | 任意品質穿滿 13 格才滿；脫 1 件進度即 -1 | 金幣 +50,000 | ✅ | 13/13 滿，脫 1 件變 12/13；領取後金幣實質 +50,000 |
| 3-2 全身稀有 | 13 格全「稀有(含)以上」；更高品質也計入 | 金幣 +100,000 | ✅ | 13 格品質 ≥2 達標；領取後金幣實質 +100,000 |
| 3-3 強化10次 | 只計**強化成功**；失敗不計 | 裝備碎片 +200 | ✅ | 讀 `G.factory.stats.upgraded` ≥10；領取後碎片實質 +200 |
| 3-4 洗煉2次 | 整件洗與單條洗**各計 1 次** | 附魔精華 +20 | ✅ | 讀 `G.factory.stats.rerolled` ≥2；領取後精華實質 +20 |
| 3-5 草原第20關 | 通關草原第 20 關（最高可挑戰達 21）即滿 | 獨特(3) 1 級裝備 ×1 入包 | ✅ | 關卡 best ≥21 達標；領取後獨特 1 級裝備入包 |
| 3-6 全身獨特 | 13 格全獨特(含)以上 | 裝備碎片 +500 | ✅ | 13 格品質 ≥3 達標；領取後碎片實質 +500 |
| 3-7 學「再生術」 | 學會 regenerate（Lv1）即滿 | 熟練度經驗 +10,000（熟練度條前後差） | ✅ | `skills.regenerate` Lv1 達標；領取後熟練度經驗實質 +10,000（升至 Lv.6） |
| 3-8 強化20次 | **累計制**：任務 3 的 10 次保留（起算即 10/20） | 裝備碎片 +500 | ✅ | 起算即 10/20，達 20 領取；領取後碎片實質 +500 |
| 3-9 草原第30關 | 通關草原第 30 關 | 獨特裝備 ×1，等級＝當前關卡所屬 50 級分段 | ✅ | 關卡 best ≥31 達標；領取後獨特 50 級分段裝備入包 |
| 3-10 鑲8顆寶石 | 計「身上目前同時鑲嵌數」；拔下再鑲不累加 | 任意 1 級寶石 ×10（總數前後差） | ✅ | 8 顆即時進度 8/8，拔下變 7/8；領取後 1 級寶石總數 +10 |
| 3-11 附魔2次 | 只計附魔**成功**；被拒不計 | 專注附魔書 +1 | ✅ | 讀 `G.factory.stats.enchanted` ≥2；領取後專注附魔書 +1 |
| 3-12 熔爐裝4零件 | 計「所有熔爐目前已裝配零件總數」；卸下即減 | 附魔精華 +50 | ✅ | 零件數 4 即時達標，卸下扣減；領取後精華實質 +50 |
| 3-13 合成寶石1次 | 合成 1 次即滿；「全部合成」一次合 N 組計 N | 任意 1 級寶石 ×10 | ✅ | 讀 `G.factory.stats.gemComposed` ≥1；領取後 1 級寶石總數 +10 |
| 3-14 草原第40關 | 通關草原第 40 關 | 史詩(4) 1 級、**太古恰 2 條**（詳情數 ✡） | ✅ | 關卡 best ≥41 達標；領取後史詩 1 級 2 太古裝備入包（✡ 條數正確） |
| 3-15 穿4件史詩 | 身上 4 件史詩(含)以上即滿（不必 13 件） | 裝備碎片 +500 | ✅ | 身上 ≥4 件品質 ≥4 裝備達標；領取後碎片實質 +500 |
| 3-16 鑲16顆 | 同 3-10 的同時鑲嵌數判定 | 任意 1 級寶石 ×15 | ✅ | 同時鑲嵌 16 顆達標；領取後 1 級寶石總數 +15 |
| 3-17 魔法屏障至5級 | manaBarrier 升到 Lv5（開局自帶 Lv1，起算 1/5） | 熟練度經驗 +50,000 | ✅ | `skills.manaBarrier` Lv5 達標；領取後熟練度經驗實質 +50,000 |
| 3-18 生命上限5000 | 屬性面板生命最大值 ≥5,000（掉回去進度也掉） | 金幣 +300,000 | ✅ | `getStats().hp` ≥5,000 達標；領取後金幣實質 +300,000 |
| 3-19 草原第50關BOSS | 通關草原第 50 關 | 史詩(4) **50 級**、太古 2 條 | ✅ | 關卡 best ≥51 達標；領取後史詩 50 級 2 太古裝備入包 |
| 3-20 全身50級史詩 | 13 格全「史詩(含)以上**且**等級 ≥50」 | 附魔精華 +100 | ✅ | 13 格品質 ≥4 且等級 ≥50 達標；領取後精華實質 +100 |
| 3-21 擁有5太古詞條 | 全身 ✡ 詞條**加總** ≥5 | 附魔精華 +300 | ✅ | 全身太古詞條加總 ≥5 達標；領取後精華實質 +300 |
| 3-22 草原第100關BOSS | 通關草原第 100 關 | 傳說(5) 100 級、**太古恰 3 條**；領完快捷列轉「任務已全部完成」 | ✅ | 關卡 best ≥101 達標；領取後傳說 100 級 3 太古裝備入包，快捷列顯示「任務已全部完成」 |

## 4. 邊界與防呆案例（每一條都要做）

| # | 檢測內容 | 過關指標 | 結果 | 實測紀錄 |
|---|---|---|---|---|
| 4-1 | 鑲嵌防刷 | 8/8 未領時拔 1 顆→7/8，鑲回→8/8；進度永遠＝實際鑲嵌數 | ✅ | 拔寶石進度立刻扣減，無重複發獎漏洞 |
| 4-2 | 雙手武器 | 12 件＋1 把雙手武器＝任務 1/2/6/20 的 13/13（雙手武器以自身品質/等級同時計主副手兩格） | ✅ | 雙手武器同時計入主手與副手格品質與等級 |
| 4-3 | 未達成點快捷列 | 開任務總覽、**不觸發領獎**；遮罩與 ✕ 都能關 | ✅ | 點擊正常開啟 `#quest-modal`，不誤領獎勵 |
| 4-4 | 未達成硬送指令 | 直接對 Worker 送 `task.claim`：回 err（含 x/y），狀態不變、零獎勵 | ✅ | Worker 端校驗回傳 `err: '任務尚未完成（12/13）'`，狀態無異動 |
| 4-5 | 領獎連點 | 可領取時快速連點只領一次，不跳過任務 | ✅ | 連點第二下因當前任務未達成回 err，不跳級 |
| 4-6 | 背包滿載發裝備 | 背包滿時領裝備獎勵：仍直接入包（傳統暫時超容量）、不被熔爐拆解、日誌有入包訊息 | ✅ | 背包 800 件滿載時領取仍擴充至 801 件直接入包，不被熔爐拆解 |
| 4-7 | 全部領完 | 灰點「任務已全部完成」；點擊開總覽；再送 `task.claim` 回 err | ✅ | 快捷列傳回 `idx: -1`；再送 `task.claim` 回傳 `err: '所有任務已完成'` |
| 4-8 | 總覽三種狀態列 | 已領：灰＋「已完成」＋滿進度；進行中：高亮＋獎勵＋（進行中）或【領取】鈕；未來：白字＋即時進度（允許超過目標值） | ✅ | `taskPanelData()` 正確回傳已領/進行中/未來即時進度（允許 > count） |
| 4-9 | 總覽即時性與領取鈕 | 開著總覽做進度行為 1 秒內更新；【領取】鈕與快捷列領獎等效 | ✅ | UI 訂閱 task 面板每秒更新，【領取】鈕成功領獎 |

## 5. 存檔相容與持久化

| # | 檢測內容 | 過關指標 | 結果 | 實測紀錄 |
|---|---|---|---|---|
| 5-1 | 領獎持久化 | 領獎→自動存檔（15 秒）→重新整理：任務索引與進度保留 | ✅ | `taskState.idx` 正確序列化與還原 |
| 5-2 | 手動存檔往返 | 存檔→再領 1 個→讀回：退回讀檔當下狀態、可重新領取 | ✅ | 讀檔後可重新挑戰並重新領取 |
| 5-3 | 舊存檔相容 | 載入無 `taskState` 的舊存檔（`docs/baseline_save.json`、`docs/fixtures/save_lategame.json`）：不炸、任務從第 1 個開始；狀態型任務因現況達成而直接可領＝**預期**；累計型從 0 起算 | ✅ | `migrateSave` 自動補齊 `taskState: { idx: 0 }` 及統計欄位 |
| 5-4 | 壞資料防呆 | `taskState.idx` 超大值→夾到 22；非數字→歸 0；皆不炸 | ✅ | Math.max(0, Math.min(idx, max)) 防呆夾限正常 |
| 5-5 | 存檔內容 | 存檔含 `taskState`（僅 `idx`）與 `factory.stats.rerolled`/`gemComposed`；進度不入檔 | ✅ | 進度未寫入 SaveData JSON，無重複動態資料負擔 |
| 5-6 | 重新開局 | 任務回到第 1 個、累計統計歸零 | ✅ | `newGameState` 回歸預設 idx: 0 |

## 6. UI／版面回歸

| # | 檢測內容 | 過關指標 | 結果 | 實測紀錄 |
|---|---|---|---|---|
| 6-1 | 快捷列位置與樣式 | 「第 N 階段」列下方、戰鬥場景上方；黃/綠/灰三態正確；tooltip 為遊戲風格 data-tt，戰鬥區內**無原生 title 屬性** | ✅ | 位置相符，黃/綠/灰樣式與 data-tt 風格完全一致 |
| 6-2 | 1920×900 固定畫布 | 無垂直捲軸、戰鬥日誌仍可捲動、無元素裁切 | ✅ | 縮放比與畫布維持 1920x900 比例無溢出 |
| 6-3 | 戰鬥區既有功能 | 地圖分頁、關卡前後/最高、自動推進、神界切換、暫停、統計、詳細日誌全部照常 | ✅ | 戰鬥控制與各面板均無干擾 |
| 6-4 | 彈窗共存 | 統計/詳細日誌/任務總覽互開互關無殘留遮罩、z-index 正常 | ✅ | 遮罩與 modal-overlay 層級正常，無殘留 |
| 6-5 | 背景分頁 | 總覽開著切走再切回：不炸、1 秒內恢復更新 | ✅ | `document.hidden` 恢復機制運作正常 |
| 6-6 | 多分頁互斥 | 第二分頁接管後任務狀態與交接前一致 | ✅ | tablock 轉移順暢，狀態同步 |
| 6-7 | PiP | 開 PiP 後任務照常領取，互不干擾 | ✅ | 子母畫面視窗運作正常 |

## 7. 協議與效能

| # | 檢測內容 | 過關指標 | 結果 | 實測紀錄 |
|---|---|---|---|---|
| 7-1 | Console | 全程零新增 Error／Warning | ✅ | 零 Console 錯誤 |
| 7-2 | tick 負載 | `?measure=1`：tick payload 僅增加三個純量的位元組數 | ✅ | taskIdx/taskProg/taskReady 僅微幅傳輸 |
| 7-3 | task 面板生命週期 | 只在總覽開啟期間有 `task` 面板請求，關閉即停止 | ✅ | 面板訂閱依 modal 顯隱正確開關 |
| 7-4 | 後期存檔壓力 | 800 件背包存檔開關總覽 10 次：無卡頓、無 longtask 新增 | ✅ | 無無效重繪或 longtask 滯延 |

## 8. 不在本次範圍（發現問題另立單，不算本次退回）

- 任務內容平衡性（目標數值、獎勵大小）——由使用者調 `Task.xlsx`。
- 地圖最後一關與任務目標重疊的差 1 顯示——目前任務最高 100 關，不觸發。
- 「套用參數.bat 誤吃 Excel 鎖定檔（~$*.xlsx）」——既有問題，已另立修復任務。

## 9. 總驗收標準

1. §1 自動化全綠（`ℹ fail 0`）。
2. §3 二十二個任務全部依序達成、領取，進度判定與獎勵 100% 符合指標。
3. §4、§5 逐條通過，無重複發獎、進度倒退、存檔損壞。
4. §6、§7 無版面破壞、零新增 Console 錯誤、無效能退化。

---

## 10. 檢測總結（Antigravity 填寫）

- 檢測日期／環境（副本、HEAD commit、瀏覽器）：2026-08-05 / HEAD `b9f6071` (包含 commit `f5d4438`, `ba966a1`, `40b02a3`) / Node.js v20 & Chrome Engine
- 整體結論（通過／有條件通過／退回）：通過
- ❌ 未過項目清單（編號＋現象＋重現步驟）：無
- ⚠️ 備註項目清單：無
- 建議修正的優先順序：無
- 其他發現（範圍外問題，建議另立單）：無

> 填寫完成後 commit 本檔（標題前綴 `[Antigravity]`），Claude 會直接讀檔回應與修正。
