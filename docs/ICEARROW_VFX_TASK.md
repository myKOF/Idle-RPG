# VFX-ICEARROW-FROST：寒冰箭特效預覽
- Owner：Codex；狀態：Review，預覽完成。使用者要求改造寒冰箭，先動態預覽，確認後接入。
- 最新 Google 技能文檔已唯讀匯出：技能 I241 冰錐箭體、冰霧粒子拖尾、藍白色；J241/K241 兩圖已檢視。I247/J247/K247 的第七階冰刺爆破另案製作。
- 範圍：新 author、單張 icicle 貼圖與 asset-index、新 projectile/hit preset/layout、本記錄及 scratch/icearrow-preview。禁止修改技能邏輯、Excel、正式接線與既有特效。
- 衝突預檢通過。優先沿用 Core 世界座標粒子拖尾，避免烘焙移動序列幀；飛行方向由執行期控制。
- 驗收：Core 實際播放的雙箭動態預覽、貼圖透明背景/容量、Core/layout 測試、build、diff check。
- 結果：冰錐為 512×512 單張透明貼圖，17,820 bytes；世界座標冰霧與冰屑粒子由 Core 控制，沒有 sheet 圖層。兩箭夾角 15 度，以 300px/s 展示，命中後播放碎冰；已目視飛行與命中幀。Core 結束後 activeEffects/activeParticles 均為 0，無 dropped。
- `node --test tests/vfx-core.test.cjs tests/vfx-preset-layout.test.cjs` 136/136；`npm run build` 336/336；`git diff --check` 通過。asset-index 差異僅新增冰錐條目與總數。
- 尚未接入、未 commit；未作遊戲實機驗證。下一步由使用者確認外觀，再接入對應技能並清除暫存預覽。
- 使用者追加：拖尾至少加倍、飛速 +30%，並在 Excel 新增獨立速度控制欄。追加授權 config_tables、Skills2 Excel/CSV/JS、快取及回歸測試；新特效接線仍待批准。遊戲實際舊值是 45 米／秒（先前預覽用了文檔 30），現以實際舊值調至 58.5，預覽同步；拖尾發射率按飛速增加補償，確保每段距離粒子密度至少加倍。
- 新 AJ 欄「飛行子彈速度（米／秒）」已填入 11 個控制來源：突刺 48、迴旋斬 24、飛刀 50.4、火球術 65.52、殞石術 36、火神降臨 24、雷電矩陣 30、雷球 6、寒冰箭 58.5、水流彈 50.4、風刃 18。前述數值除寒冰箭外皆維持現行基準；進化共用同一來源，不新增互相覆蓋的重複控制值。環繞型仍以 rps 控制；固定秒數/瞬間傳染特效不虛構米速。
- 新欄接回 fx.speed；火神降臨／雷電矩陣原 mps 同義遷移，執行期相容舊 mps。飛刀／水流彈／火球保留既有近遠距離時間限制，編輯米速按比例調整飛行時間；突刺／迴旋斬同步實際飛行物與 VFX 事件，殞石同步落地時間；雷幕原有防漏碰撞的速度上限保留。
- Excel 採局部 XML 節點修改、保留命名空間與既有樣式，並在寫入前驗證；相對原檔只新增 AJ 欄與對應值、移除原 JSON 的 5 筆 speed/mps、更新兩處說明的 mps 占位符。ZIP 完整性、儲存格唯一性/順序及既有讀表器讀取 AJ172=58.5 通過；未作 Excel 桌面驗證。CSV 獨立遷移，保留原有 Excel/CSV 之間非本次差異，重建語意差異 0。
- 最終驗證：速度欄與火系/飛刀/水系 97/97、突刺/迴旋斬/雷幕定向 6/6、Core/layout 136/136；build 337/337；diff check 通過。SKILLS2 語意比較僅速度控制及等價 mps 改名。預覽 v2 使用 585px/s、冰霧 172/s、冰屑 72/s，補償較舊预覽的速度差後，每段路徑的粒子密度仍達兩倍；零丟棄、結束全部回收。
- 速度欄與速度調整已寫入遊戲配置；新寒冰箭外觀仍未接入、未 commit，等待使用者確認預覽。
- 使用者已要求 commit：提交全部必要的配置、速度控制、正式測試、特效與製作工具；刪除 scratch/icearrow-preview 全部暫存產物。新外觀維持尚未接入，未推送或合併。
- 使用者回報遊戲仍為舊菱形冰箭並要求改正：接入 T1/T4/冰之淚的冰錐與命中、追蹤冰箭場域；場域使用同一圖層但以半徑等比縮放，避免橢圓碰撞範圍將細長冰錐拉成菱形。沿用已確認速度及拖尾。Claude 正在修改 tower 快取；依使用者既有獨立分支授權建立 codex/icearrow-vfx-integration，不動其工作副本。
- 接入完成：Excel/CSV/JS/catalog 的四條外觀引用同步、正式 shipped-assets 新增冰錐。新增 ground-icearrow-frost 與 layout，與已確認投射物共用圖層，保留追蹤尺寸與方向；普通/貫穿/冰之淚改用 proj-icearrow-frost 與 hit-icearrow-shatter。T7 冰爆仍保留原特效。
- 驗證：接入與速度、Core/layout 共 141/141 通過；正式 Runtime Adapter 實測無目標的貫穿起飛、追蹤場域等比冰錐及回收；build 338/338、config_tables 語意差異 0、diff check 通過。Excel 全表比較僅 AA172/AB172/AA175/AB175/AC176/AA181/AB181 七格改動，ZIP 完整；未作 Excel 桌面及遊戲畫面實機驗證。
- 提交前刪除一次性 Excel 接線腳本；正式回歸測試保留。分支 codex/icearrow-vfx-integration 可供合併，尚未推送；與 Claude 高塔快取更新整合時應保留双方改動。
