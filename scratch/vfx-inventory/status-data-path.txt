panelEnemyFields: PANEL 'battle' is NOT a projection: js/worker/sim.worker.js:753-762 returns `{ gt: GT, field: FIELD, tower: TOWER, stage, zoneProgress, dropRates, zones, runStats, lootStats }` — `field` is the whole runtime FIELD object structured-cloned (FIELD def js/combat.js:5-12: player, monster, monsters[], spawnCd, reviveCd, dpsWindow; plus runtime playerPos, mapComplete, _gmArena, _waveClearPending). Each FIELD.monsters[i] entry (spawn literal js/combat.js:264-286, GM arena 329-345) carries: name, emoji, npcId, appearance, img/imgFailed, level, maxHp, hp, atk, def, mdef, magic, attr, aspd, dodge, hit, elite, isBoss, gold, xp, atkCd, ctrlRes, _spawnAt, _stage, _enterCd, shield, shieldMax/shieldMaxVersion, **effects:{ key -> untilGT }**, **buffs:{ key -> { val, until, dur, ext, sid, unit, stacks } }** (combat.js:518-519; shield entry 569-570), **dots:[ { dps, until, name, dur, ext, sid, interval, acc, unit, stacks, frostFormula, bodyDmg, frostMult, sourceName, sourceKey, sourceLevel } ]** (combat.js:665-670), plus runtime-added floatSel 'mv-float-N' (combat.js:183-193, FIELD_ENEMY_FLOAT_SEQ monotonic), pos {x,y} world coords (js/battlefield.js:171), _rewarded, _deathClearCd, _sgAcc/_sgBurnWatch/_sgFrozenWatch/_sgDotSkipAt (skills2 metronome timestamps), _dotHasteUntil/_dotHasteMult. There is NO `cell` field any more — the grid was abolished (battlefield.js:13-31); BF_COLS×BF_ROWS (default 4×4 = 16) is only the on-field enemy capacity (battlefield.js:28-32,178). Renderer reads d.floatSel, d.hp, d._rewarded, d._deathClearCd, d.name, d.level, d.isBoss, d.elite, d.atkCd, d.pos, d.attr, d.img, d._enterCd (battle-renderer.js:1111-1147, makeEnemy 783-909) and derives the status strip text from the full status containers via statusTextOf(d) → entStatus(d) (battle-renderer.js:773-782, 1146-1147). Per-entity structured status lists (sid/until/stacks/dps/val) ARE already in the snapshot; only the text string is derived on the main thread.
panelPlayerFields: FIELD.player = newPlayerEntity (js/combat.js:47-49): { hp, mp, shield, shieldMax, shieldMaxVersion, atkCd, _targetSwitchCd, skillCds:{}, buffs:{}, dots:[], effects:{}, _lastStandAt, _skillCastRemaining, _skillCastId } (+ _lockTarget clone). Revive countdown is FIELD.reviveCd, player world position FIELD.playerPos (battle-renderer.js:1160-1163, 1198-1202). High-frequency vitals come separately in TICK view (buildView sim.worker.js:405-448; allowed keys protocol.js:181-188: hp,hpMax,mp,mpMax,shield,gt,simT,paused,towerActive,...) — NO status data in TICK view. BattleRenderer takes vitals from TICK (battle-renderer.js:5662-5674 → S.vitals) and everything else from PANEL battle (5676-5678). DOM HUD: renderBattle merges panel field.player with TICK hp/mp/shield (ui.js:3810-3826), pv-status text = entStatus(p) (ui.js:3834), buff badges renderBattleBuffBar(pEnt, snapshotGt) via statusEntries (ui.js:3025-3110, badge key = JSON [sid,effect,kind] at 2972-2974, remaining = until − gt). Tower: PANEL 'tower' = { gt, tower: G.tower, runtime: TOWER } (sim.worker.js:884-885); TOWER.boss literal has effects/buffs/dots (js/tower.js:30-39, towerBoss:true), TOWER.player = newPlayerEntity (tower.js:68); UI uses entStatus(b)/entStatus(p) at ui.js:6119/6123 (targets tb-float/tp-float).
statusInSnapshot: Exactly what exists today: the raw simulation containers ent.effects (key→until GT timestamp), ent.buffs (key→{val,until,dur,ext,sid,unit,stacks}), ent.dots ([{dps,until,name,dur,ext,sid,interval,acc,unit,stacks,...}]) for every FIELD.monsters entry and FIELD.player, delivered verbatim inside PANEL 'battle'.field (built at js/worker/sim.worker.js:753-762 — `field: FIELD`, no projection) and, for tower fights, PANEL 'tower'.runtime (sim.worker.js:884-885 — TOWER.boss / TOWER.player). No pre-built per-entity status list and no text string is sent by the worker. On the main thread js/status.js:239-285 statusEntries(ent) (shared file loaded on both threads) turns those containers into [{ sid, name, icon, kind, effect, until, remain(=until−GT), dur, val, dps, stacks }] ordered shield → ctrl(effects) → dots → buffs; GT on the main thread is extrapolated from the TICK view by uiApplyGameTime (js/ui.js:374-377, called at uiTick ui.js:6232) so `until > GT` works on the clone. Consumers: ui.js:2883-2893 entStatus(ent) → "icon+name(+↑↓)(+stacks)" joined string; ui.js:2761-2778 enemyBuffTooltipDesc; ui.js:3025-3110 renderBattleBuffBar (player badges); ui.js:3834/3889/3925 pv-status & DOM enemy-card .enemy-status; ui.js:6119/6123 tower tb-status/tp-status; canvas js/battle-renderer.js:773-782 statusTextOf(data) = entStatus(data) with HTML stripped and truncated to 14 chars, written to ent.statusText (PIXI.Text) in syncBattle at 1146-1147 — that PIXI.Text is the ONLY status visual on the canvas today (no aura/particles). Protocol docs: protocol.js:60-98 (PANEL {name,data}, TICK {view,dirty,events}, VISUAL {events}); protocol.js:110-166 VFX event fields (fxKind/variant/elem/cat/targets/area/dur/count/travelMs/delayMs...), no status fields.
applyHooks: Authorized entry: js/status.js:211-237 applyStatus(ent, sid, ctx) → switch(def.effect): 'dot' → combat.js applyDot (608-671); 'hot'/'stat' → applyBuff (504-521); 'ctrl' → applyEffect (454-464); 'shield' → applyShield (557-572). BUT applyStatus is not the only path: status.js:414-431 applyStatusRef calls applyDot directly for table-less fusion DoTs (sid ''), and legacy call sites still call the low-level writers directly (grep counts of applyStatus/applyStatusRef/applyDot/applyBuff/applyEffect/applyShield/applyPoison: skills.js 14, skills2.js 64, legendary.js 4, combat.js 8, potential.js 6, status.js 8). Therefore the single reliable hook is inside the four low-level writers in js/combat.js, where fresh-vs-refresh is already decidable: applyDot — loop 640-660 finds an existing dot by name and mutates it (refresh, `return` at 660); fresh instance is the `ent.dots.push` at 665-670 (note applyDot returns undefined in both cases; the legendaryInstantBurn early-return at 610-613 and invuln at 609 produce no status). applyBuff — 512-513 `prev = ent.buffs[key]; stackStep(stackCfg, prev && prev.until > GT ? prev : null, val)`: fresh iff !(prev && prev.until > GT); write at 518-519 (stack count available as st.stacks). applyEffect — 462 `ent.effects[key] = GT + dur`: fresh iff previous `(ent.effects[key]||0) <= GT` before the write (ctrl keys stun/slow/invuln + sg ctrl keys). applyShield — 562 `prev = ... shield.until > GT ? shield : null`: fresh iff !prev; write 569-570. Target id for the event: enemies have ent.floatSel ('mv-float-N') and tower boss ent.towerBoss → 'tb-float' (pattern already used by enemyAttackSourceId combat.js:1096-1100); player must be resolved by identity (ent === FIELD.player → 'pv-float', ent === TOWER.player → 'tp-float'; helpers util.js:14-16 playerEventFloatTarget, 37-41 enemyEventFloatTarget). Emission path: playCombatVfx / shimPushEvent('vfx', spec) (shim.js:76-101) — 'vfx' is classed urgent (shim.js:71-74) and posted every 100 ms sim step by emitUrgentVisualEvents (sim.worker.js:330-334, called 343/362), cap 80/batch (shim.js:38,80). Today apply-time visuals exist only indirectly: regular skills choose fxKind 'curse'/'selfBuff' at CAST time from the skill's status refs (skills.js:2486-2499 skillVfxKind), and skills2 hand-emits curse/bleed & curse/poison after applying bloodblade DoTs (skills2.js:2707, 2711-2714, 9904); mire re-applies (refresh) sgMire/sgMirePoison/sgMireLava/sgMireBleed/sgPlague to every victim every ground tick (skills2.js:5338-5343) — a fresh-only hook filters these out automatically.
tickHooks: Generic DoT settlement: js/combat.js:680-763 tickStatuses(ent, dt). Per-dot interval accumulation at 699-716 (d.acc, `while (elapsed >= d.interval)`), the exact "this dot ticked now" point is 717-724 (`if (seconds > 0 && d.dps > 0)`, known: ent, d.sid, d.name, stacks, dDmg); all dots' damage is aggregated and applied once at 731 (applyEnemyHpDamage) with a single log line 750-751. dtEff can be multiplied by _dotHasteMult / passiveDotHaste (686-694) so ticks may exceed 1/interval. Shield expiry at 766-782 tickShieldExpiry (natural end of a status = hook for 'expire' visuals; dots expire silently at 703-705/727-728, buffs/effects just stop passing `until > GT`). Callers: field enemies combat.js:1471-1473 (every 100 ms sim step for each combat-ready enemy), field player 1375, tower player/boss tower.js:157-158. Separate skills2 metronomes deliberately bypass tickStatuses (comments skills2.js:3089-3091, 9760-9762) using e._sgAcc[sid]: sgTickBurn 3092-3140, sgTickFrost 6975-7020, sgTickBloodDots 9763-9810 — invoked from the skills2 scheduler at skills2.js:9561/9567-9568 (via tickSkillSchedulers skills.js:61 ← combat.js:1413). These are where today's tick VFX are emitted, so a generic hook at combat.js:717 would double-fire for sgBurn/sgFrostBite/sgBleed/sgPoison unless one side is suppressed. Recommended single hook: at combat.js:717 push (targetId, d.sid, elem, stacks) into a per-sim-step buffer; flush once per step (end of fieldTick / towerTick or right before shimDrainUrgentVisualEvents in emitUrgentVisualEvents sim.worker.js:330-334) as ONE event per sid with targets[] (≤8, matching sgEmitVfx cap skills2.js:1391-1395 and onVfx slice battle-renderer.js:4593).
existingTickVisuals:
  - [0]
    js/skills2.js:3136-3139 sgTickBurn → sgEmitVfx('fireball', tickedNow, ..., { fxKind:'impact', variant:'burn-tick', elem:'fire' }) — all enemies ticking in the same sim step batched into one event (comment 3096-3098)
  - [1]
    js/skills2.js:7016-7019 sgTickFrost → { fxKind:'impact', variant:'frost-tick', elem:'ice' } — batched per step like burn (comment 7013-7014)
  - [2]
    js/skills2.js:7746-7749 waterball frost apply (not a tick) → { fxKind:'impact', variant:'frost-tick', elem:'ice' } on freshly frosted victims
  - [3]
    js/skills2.js:9805-9808 sgTickBloodDots → { fxKind:'impact', variant: sid==='sgPoison' ? 'poison-tick' : 'bleed-tick', elem: poison?'poison':null } — ONE event per enemy per dot per tick (not batched); only runs when bloodblade tier5 spread or tier7 zero-day is learned (early return 9766)
  - [4]
    js/skills2.js:9790-9792 poison spread → { fxKind:'chain', variant:'poison-spread', elem:'poison', count:1 } targets [source, victim]
  - [5]
    js/skills2.js:2707 bloodblade apply → { fxKind:'curse', variant:'bleed' }; 2711-2714 → { fxKind:'curse', variant:'poison', elem:'poison' }; 9904 venom field apply → curse/poison on all victims
  - [6]
    js/skills2.js:5316-5325 sgMireGroundTick → { fxKind:'aura', variant:'mire'|'mire-poison'|'mire-lava'|'mire-lava-poison', elem, dur:f.gap, area } re-sent EVERY ground tick (gap 0.5s poison / 0.4s lava / 0.35s plague, table skills2.js:185); renderer merges by area key (protocol.js:151-152 warning, spawnMirePool battle-renderer.js:2913); 9877-9884 sgEmitBloodDomainAura re-sends aura with area.id every domain pulse
  - [7]
    js/skills.js:2486-2499 skillVfxKind: pure-debuff skills → fxKind 'curse', self-only status/heal/shield skills → 'selfBuff' (emitted at cast, not at status apply)
  - [8]
    js/vfx.js:2160-2164 DOM path synthesises vfxImpact({elem:'ice', variant:'frost-tick'}) client-side inside the 'frost-spread' chain handler
  - [9]
    Renderer handling (DOM js/vfx.js): 423-425 bleed/bleed-tick theme override (#d92846/#ffd0d8/#ff4962); 647-648 isBloodbladeNoHitShakeSpec (poison-spread/bleed/poison/bleed-tick/poison-tick suppress card shake); 755 bleed-tick → class 'vfx-impact vfx-impact-phys vfx-impact-bleed'; 756 poison-tick → 'vfx-impact vfx-impact-poison'; burn-tick/frost-tick fall to generic 'vfx-impact vfx-impact-<elem>' (757); 2336-2347 vfxCurse (🩸/☠️ glyph, classes vfx-curse-bleed/-poison, 1100 ms, hit react)
  - [10]
    Renderer handling (canvas js/battle-renderer.js): 74-76 bleed/bleed-tick theme; 1303-1307 isBloodbladeNoHitJoltSpec suppresses jolt for poison-spread/bleed/poison/bleed-tick/poison-tick; 4960-4962 'curse' → spawnCurse 4097-4123 (PIXI.Text glyph rising 16px/s, 0.9 s, hitReact at t>0.1); 4957-4958 'selfBuff' → spawnSelfBuff 4076-4096 (rising ellipse ring 0.7 s + 2 risers at player); 5002-5010 default 'impact' branch → spawnImpact(pt, spec, strong) + hitReact(id, elem, strong, suppressJolt) staggered 40 ms/target — burn-tick, frost-tick, poison-tick, bleed-tick have NO dedicated canvas branch (no 'burn-tick'/'frost-tick' string in battle-renderer.js), so they render as generic element-themed impacts
volumeRisk: Rates: sim step TICK_MS=100 ms (sim.worker.js:60); 'vfx' events are urgent (shim.js:71-74) → posted every step (10 Hz) via emitUrgentVisualEvents (sim.worker.js:330-334), FIFO cap 80/step (shim.js:38,80 drops oldest); main-thread queue max 480 with per-rAF time budget (ui.js:117-125, 527-546); measured flood 424–1165 events/s at Lv1000 with 32 enemies (ui.js:117-121). Field capacity BF 4×4 = 16 by default (battlefield.js:31-32); comments cite 32 on field. Naive per-enemy-per-tick emission: mire poison 2 Hz + lava 2.5 Hz + plague ~2.9 Hz + burn/frost/bleed/poison 1–2 Hz each (haste multipliers 686-694 push higher) → per enemy 5–15 tick events/s → 16 enemies ≈ 80–240/s, 32 enemies ≈ 160–480/s, on top of existing skills2 emitters (bloodblade already emits per enemy per dot at 9805-9808) — this alone can saturate the 80/step urgent cap and evict skill/enemy-attack VFX. Apply-time naive emission is worse for mire: sgMireGroundTick re-applies 2–5 statuses to every victim every 0.35–0.5 s (skills2.js:5338-5343) — a fresh-only filter reduces this to one burst when an enemy enters the pool. Mitigations: (1) batch ticks per sim step per sid into one event with targets[] (≤ #distinct active sids per step ≈ 5–10 events/step worst case, i.e. 50–100/s hard ceiling, typically <20/s); (2) do NOT send auras as events at all — reconcile from the 5 Hz snapshot (zero extra traffic, panel already ~2.8 KB/5 Hz per sim.worker.js:272); (3) dedupe apply events by (targetId,sid) within a step; (4) suppress the generic tick for sgBurn/sgFrostBite/sgBleed/sgPoison while the skills2 emitters remain, or migrate those emitters to the generic hook to avoid double visuals; (5) keep targets ≤8 per event (sgEmitVfx cap 1391-1395, onVfx slice 4593) and prefer area-based aura for large groups.
recommendation: Three-layer wiring with a single new fxKind and no snapshot change:
   
   A. AURA (persistent status look) — no protocol change. Reconcile from PANEL 'battle' in BattleRenderer.syncBattle (js/battle-renderer.js:1089-1226) right where statusTextOf is applied (1146-1147): for each snapshot entity call statusEntries(d) (js/status.js:239, already available main-thread; GT is synced by uiApplyGameTime ui.js:374-377 each uiTick 6232, or use uiCountdownRemain(until−gt, panel.gt) ui.js:383-388) → active set of {sid, effect, kind, elem(def.elem), stacks, until, dur}. Keep a per-entity map ent.auras[sid] → VFX Core layer instance; create on sid appearance (preset lookup 'status/<sid>' with fallback by def.effect+def.elem+def.kind, e.g. dot+fire → burning aura, ctrl+stun → stars, shield → bubble, stat buff → glow), destroy on disappearance or entity death/destroyEntity (1294-1300), drive intensity by stacks and remain/dur. Do the same for the player using field.player keyed 'pv-float' (S.player), and for tower via PANEL 'tower'.runtime.boss/player if the DOM path ever moves to canvas. Identity keys: enemies 'mv-float-N' (combat.js:183-193, monotonic FIELD_ENEMY_FLOAT_SEQ; S.entities keyed by it 95-96/871/1118-1131; worker restart resets numbering → renderer already wipes on 'workerRestarting' 5686-5703 and re-fingerprints by name/level/rank 1122-1127), player 'pv-float', tower 'tp-float'/'tb-float' (util.js:14-41). Effective rate: worker marks battle dirty every emit (markCombatDirty sim.worker.js:273-279, called 344/386) → TICK 5 Hz (TICK_EMIT_MS=200, sim.worker.js:61, 383-389) → ui.js:877-892 requestPanelData('battle', true) (persistent subscription ui.js:76) → PANEL reply → ui.js:898-910 store + battle-renderer.js:5676-5678 syncBattle; so auras appear/disappear within ≤200 ms + one round trip, which is fine for state visuals. Keep the PIXI statusText or drop it once auras exist.
   
   B. APPLY flash (fresh only) — hook in the four low-level writers in js/combat.js, not in applyStatus (legacy direct calls bypass it): applyDot fresh push 665-670; applyBuff when !(prev && prev.until > GT) at 512-519; applyEffect when (ent.effects[key]||0) <= GT before write at 462; applyShield when !prev at 562-570. Each calls a tiny emitStatusVfx(ent, sid, 'apply', stacks) that resolves targetId (ent.floatSel | ent.towerBoss→'tb-float' | ent===FIELD.player→'pv-float' | ent===TOWER.player→'tp-float', mirroring enemyAttackSourceId 1096-1100) and pushes `{ fxKind:'status', variant:'apply', sid, effect:def.effect, kind:def.kind, elem:def.elem||null, stacks, targets:[id], cat:'status' }` via playCombatVfx/shimPushEvent('vfx'). Dedupe (targetId,sid) within a sim step. Only protocol change: document fxKind 'status' in protocol.js EVENT_KINDS.VFX (110-166) and add `case 'status'` in onVfx (battle-renderer.js:4634+) and renderCombatVfx (vfx.js:2352); unknown fxKind currently falls into the default impact branch (5002-5010), so ship the renderer case first.
   
   C. TICK pulse — one hook at combat.js:717 (`seconds > 0 && d.dps > 0`) appending (targetId, d.sid, d.stacks) to a module-level per-step buffer; flush once per sim step (end of fieldTick / towerTick, or in emitUrgentVisualEvents sim.worker.js:330-334 before draining) as one `{ fxKind:'status', variant:'tick', sid, elem, targets:[≤8] }` per sid. Also covers player DoTs (1375, tower.js:157). To avoid double visuals, either skip sids sgBurn/sgFrostBite/sgBleed/sgPoison in the generic flush while skills2.js:3137/7018/9806 emitters exist, or delete those emitters and let presets 'status/tick/<sid>' replace burn-tick/frost-tick/bleed-tick/poison-tick (renderer keeps accepting the old variants for compatibility). Optional D: shield expiry (766-782) / dot expiry (703-705) → variant 'expire' if a fade-out cue is wanted; otherwise the aura reconcile in A handles disappearance implicitly.
   
   VFX Core mapping: 'status/apply' = short burst preset per effect/elem; 'status/tick' = brief pulse on the aura layer (scale/alpha spike) rather than a new impact; 'status/aura' = long-lived layered preset owned by the entity, updated from snapshot fields (stacks→particle count/tint, remain/dur→alpha curve).
