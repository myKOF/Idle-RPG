scaleGuide: UNITS. Layer `scale{x,y}` is a multiplier on the texture's native pixel size (no px-size field for sprite/particle; only procedural has `size{x,y}` px, default 256 — vfx-core.js:797-798). Pixi backend applies `node.scale.set(t.scaleX, t.scaleY)` (js/vfx-pixi-backend.js:123). Core: sprite `t.scaleX = d.scale.x * effect.scaleX * scaleXOverLife(k) * flipX` (vfx-core.js:789-790); particle `t.scaleX = p.baseScale(startScale) * effect.scale * scaleOverLife(k); t.scaleY = t.scaleX` (vfx-core.js:935-936). Position is px in effect-local space, rotated/scaled by the effect transform (`toWorld`, vfx-core.js:727-735); Pixi axes: +x right, +y DOWN. Editor `stageRoot` is centred on the canvas (editor.js:2500-2504), so preset (0,0) = effect anchor point.
   
   ALL 20 SHIPPED ASSETS ARE 512×512 (asset-index facts; also light_01/star_05 used by lightning-orb-field). So on-screen px = 512 × scale (effect.scale=1):
     0.04→20 · 0.05→26 · 0.085→44 · 0.1→51 · 0.115→59 · 0.13→67 · 0.15→77 · 0.188→96 · 0.2→102 · 0.22→113 · 0.25→128 · 0.28→143 · 0.31→158 · 0.34→174 · 0.42→215 · 0.46→236 · 0.5→256 · 0.55→282 · 0.7→358 · 0.8→410 · 0.9→461 · 1.0→512 · 1.076→551.
   Caveat: light-mask/particle-pack textures have soft radial falloff; visible glow core is ~50–70% of the sprite box. Transparent-variant content bounds: light-masks transparent/circle_a 428×430, smoke_04 459×466, splat05 454×448.
   
   OBSERVED SIZE CONVENTIONS (from presets):
   • Orb/glow trio (lightning-orb-field*.json): outer glow (circle_a / light_01) scale 0.20–0.335 (102–172 px), body (light_03) ≈0.6× glow (0.121–0.203 → 62–104 px), bright core (circle_b / star_05) ≈0.42× glow (0.085–0.142 → 44–73 px; star_05 core 0.25). Whole 7-orb field spans x −187..+188, y −100..+127 with a field-glow ellipse scale 1.076×0.739 (551×378 px), alpha 0.34.
   • Ground / floor ellipses use anisotropic scale y/x ≈ 0.31–0.55: scorch splat 0.5×0.24 (256×123 px), fire ground-ring 0.42×0.17, base-glow 0.34×0.2, demo ring 0.5×0.25, black-hole discs 0.9×0.28, 0.7×0.24, 0.5×0.17, 0.55×0.18, black-hole outer glow 1.0×0.55 (512×282 px).
   • Vertical column (fire-tornado): funnel cone_composed_c_noise scale 0.46×0.8 (236×410 px) at y −70; swirl_lower 0.34×0.26 at y +30; swirl_upper 0.5×0.36 at y −130; ground at y ≈ +104..+126; smoke emitter at y −230 → total ≈ 360–400 px tall, ~250 px wide.
   • Black hole: event-horizon opaque black disc scale 0.26 (133 px), photon ring 0.22 (113 px), inflow emitters placed at radius 150 px, debris spawn radius 210 px → ~500 px overall footprint.
   • Particle startScale ranges (× 512): trails/streaks [0.04,0.09] → 20–46 px; sparks/sparkles [0.05,0.12] → 26–61 px; embers [0.05,0.11]; flame puffs [0.1,0.2] → 51–102 px; demo sparks [0.12,0.28]; lightning arcs [0.169,0.281] → 87–144 px (≈0.9× the orb glow scale, e.g. glow 0.309 → arc [0.206,0.347], glow 0.2 → arc [0.133,0.224]); smoke puffs [0.28,0.5] → 143–256 px. scaleOverLife multiplies these.
   
   RUNTIME SCALING KNOBS: `play(id, {position, rotation, scale, seed, startTime})`; effect.scale multiplies every layer size AND every layer position/spawn offset (toWorld). NOTE: js/vfx-core.js is currently MODIFIED (uncommitted, 1140 lines vs 1079 at HEAD): an in-progress change adds `scaleX`/`scaleY` per-axis effect transform (`applyTransformParams` vfx-core.js:660-681) — positions & sprite sizes use scaleX/scaleY, particle sprite size uses `scale` (= min(|scaleX|,|scaleY|) when only axes given, vfx-core.js:679), plus `runtime.setTransform(handle, {position,rotation,scale,scaleX,scaleY})` (vfx-core.js:693-698, exported :1031) for moving projectiles/fields per frame. Beams/slashes should therefore be authored at a nominal length along +x and stretched with scaleX at play time.
curvePatterns: FORMAT. Every *OverLife is `[[t,v],…]` with t in 0..1 non-decreasing, linear interpolation, hold beyond ends (`sampleCurve` vfx-core.js:96-112); a plain number is accepted as a constant (e.g. `"rotationOverLife": 0` lightning-orb-field.json:258). Max 16 points (HARD_LIMITS.maxCurvePoints vfx-core.js:51). alpha/scale curves must be ≥0; `layer.alpha` itself must be 0..1 (vfx-core.js:204) but alphaOverLife values may exceed 1 (used as a flicker multiplier, up to 1.26) — final alpha = layer.alpha × curve. No easing/bezier by design. Per-axis `scaleXOverLife`/`scaleYOverLife` and `rotationXOverLife`/`rotationYOverLife` (cos-projection flip) are sprite/procedural only — validation rejects them on particle layers (TYPE_ONLY_FIELDS vfx-core.js:~333).
   
   FADE / ONE-SHOT ALPHA (start 0, end 0):
   • sprite ring flash: [[0,0],[0.15,0.9],[1,0]] (demo ground-ring) · smoke sprite [[0,0],[0.3,0.55],[1,0]]
   • flame puff [[0,0],[0.1,1],[0.55,0.6],[1,0]] · embers [[0,0],[0.12,1],[1,0]] · sparks instant-on linear-out [[0,1],[1,0]] · demo sparks [[0,1],[0.7,0.8],[1,0]]
   • streak/trail [[0,0],[0.15,1],[0.85,0.9],[1,0]] · debris hold [[0,0],[0.3,0.85],[0.7,0.85],[1,0]]
   • lightning arc pop [[0,0],[0.05,1],[0.25,0.7],[1,0]] or [[0,0],[0.06,1],[0.3,0.85],[1,0]] · sparkles [[0,0],[0.2,1],[1,0]] · smoke particles [[0,0],[0.25,0.4],[1,0]]
   Rule of thumb: attack ≤10–15% of life, release from 55–85%.
   
   LOOP-SAFE PULSES (first value == last value so the wrap is seamless):
   • breathing glow scaleOverLife triangular: [[0,0.95],[0.5,1.08],[1,0.95]] (±6%), [[0,0.9],[0.5,1.15],[1,0.9]] (±12%), [[0,0.98],[0.5,1.04],[1,0.98]] (±3%, photon ring)
   • ground-ring re-flash each loop: alphaOverLife [[0,0.15],[0.12,0.85],[0.6,0.4],[1,0.15]] + scaleOverLife [[0,0.7],[1,1.25]] (fire-tornado.json:39-66) — note scale is NOT seamless (snaps back), intentional "re-emitted ring".
   • lightning flicker (13 keyframes at 1/12 steps, 2 cycles per 4 s loop): alpha 1,1.225,1.225,1,0.775,0.775,1,… (±22.5%) with scale 1,1.078,1.078,1,0.922,0.922,1,… (±7.8%) for the glow; body alpha 1.096,1.136,1.04,0.904,0.864,0.96 (±13.6%) with scale ±4.3%; core alpha 1.151,1.214,1.064,0.849,0.786,0.936 (±21%) with scale 1.12,1.06,0.94,0.88,0.94,1.06 (±12%, anti-phase to alpha). Orbs B–G are phase-shifted copies of the same waveform (different start index) so the field never blinks in unison. Sine-like: 6 samples per cycle.
   • fire funnel sway rotationOverLife [[0,-0.05],[0.5,0.05],[1,-0.05]] (±2.9°).
   
   GROW / SHRINK (scaleOverLife):
   • expanding shockwave ring [[0,0.4],[1,1.6]] (demo) or [[0,0.7],[1,1.25]] · smoke sprite grow [[0,0.6],[1,1.3]] · smoke particles [[0,0.7],[1,1.8]]
   • flame grow [[0,0.3],[0.55,1],[1,1.5]] · lightning arc grow [[0,0.85],[0.25,1.05],[1,1.15]]
   • shrinking trails/sparks [[0,1],[1,0.45]], [[0,1],[1,0.35]], [[0,1],[1,0.3]] · twinkle [[0,0.6],[0.35,1.15],[1,0.7]]
   • jitter "fake lightning" 5-pt noise [[0,0.85],[0.2941,1.334],[0.5245,0.614],[0.799,1.51],[1,0.424]] paired with rotationOverLife 7-pt ±0.1 rad noise (lightning-orb-field.json:446-497).
   
   ROTATION (radians; rotationOverLife is additive to layer.rotation and, for particles, to rotationStart+rotationSpeed·t):
   • continuous spin per loop = 2π·N: 6.28318 (1 rev), 12.56637 (2), 25.13274 (4), −12.56637, −18.84956 (−3, counter-rotating). Parallax rule (black-hole): outer ring 1 rev, swirl_outer 2, swirl_inner 4, inner accretion −3 → inner spins faster and opposite. Fire swirls: lower +1 rev, upper −2 rev.
   • slow drift [[0,0],[1,0.6]] (smoke).
   • rotationXOverLife/rotationYOverLife: scaleY/scaleX *= cos(θ) (vfx-core.js:779-782) — 0→π gives a card flip / mirrored back; useful for spinning rings/blades.
particlePatterns: REQUIRED FIELDS: `emission` ({mode:'rate', rate} or {mode:'burst', count}), `lifetime` (number or [min,max] s), `assetId` (validateParticleLayer vfx-core.js:~245-305). Defaults (layerDefaults vfx-core.js:453-495): speed 0, direction −90 (deg, = screen UP; 0 = +x right, 90 = down), spread 0 (full cone width; angle = direction + (rng−0.5)·spread, vfx-core.js:824 → 360 = omnidirectional), gravity {0,0} px/s², startScale 1, rotationStart 0 / rotationSpeed 0 (rad, rad/s), spawn {shape:'point'} (also 'circle' radius — uniform disc via sqrt(rng) — and 'box' width/height), alignToVelocity false, velocityRotationOffset 0, anchor 0.5/0.5, blendMode normal, tint #ffffff, alpha 1. Range fields accept number or [min,max]. Particle scaleY always equals scaleX; tint is per-layer (all particles share). RNG seeded by effect.seed + layerIndex·0x9E3779B9 → reordering layers changes randomness (that's why the Editor keeps display order in vfx/layouts/*.json).
   
   BUDGET: DEFAULT_BUDGET maxActiveEffects 24, maxParticles 1200 global, perEffectParticleLimit 300 (vfx-core.js:75-79); per-layer cap = min(layer.maxParticles, perEffectParticleLimit); hard limits 2000/layer, rate ≤ 2000/s, 32 layers. Dropped particles are counted, never crash. Presets keep each layer ≤120 and whole preset ≈ 170–300 particles.
   
   RATE SIZING RULE: maxParticles ≈ rate × max lifetime × 1.1–1.5 (steady-state): flame 60/s × 1.5 s → 120; embers 26/s × 1.6 → 45; fire sparks 18/s × 0.7 → 30; smoke 12/s × 2.3 → 30; black-hole inflow 20/s × 1.3 → 34 (×4 emitters); debris 6/s × 3 → 24; sparkles 9/s × 0.5 → 30. Deliberate starvation for flicker: lightning arc rate 26 with cap 5 (lifetime 0.1–0.4), or rate 12 cap 10 (lifetime 0.09–0.24).
   
   BURST (one-shot hit): demo-basic sparks — emission {mode:'burst', count:24}, maxParticles 48, lifetime [0.45,0.9], spawn circle r 8, speed [60,140], direction −90 spread 110, gravity y +90, startScale [0.12,0.28], rotationStart [0,6.28], rotationSpeed [−3,3], alpha [[0,1],[0.7,0.8],[1,0]], scale [[0,1],[1,0.3]], blend add, tint #ffd27f. Burst re-fires on every loop wrap (burstDone reset, vfx-core.js:972-973). Effect ends only when time ≥ duration AND no particles remain (vfx-core.js:980-982), so one-shots never cut particles off.
   
   RECIPES:
   • FIRE COLUMN (fire-tornado.json:184-329): flame_04 core at ground y 112, spawn r 10, speed [150,270] up, spread 34°, gravity y −25 (extra lift), rotationStart [0,2π], rotationSpeed ±2.2, startScale [0.1,0.2], grow 0.3→1→1.5, tint #ff7a1e add. Embers trace_02: speed [200,340], spread 26, gravity −20, startScale [0.05,0.11], rotationSpeed ±4, shrink to 0.35, tint #ffc061. Sparks spark_01: lifetime [0.35,0.7], spread 100°, gravity +90 (fall), speed [60,150], tint #fff0b8. Smoke smoke_04 (normal blend, tint #5c5148): emitter at top y −230, spawn r 70, speed [15,45], spread 60, gravity −18, lifetime [1.5,2.3], startScale [0.28,0.5], grow 0.7→1.8, alpha peak 0.4.
   • SUCTION / INFLOW SPIRAL (black-hole.json:119-394): 4 emitters at (±150,0),(0,±150); each gravity 170 px/s² toward centre, initial direction = tangent+25° (25,115,205,−65) so paths spiral in; speed [30,60]; spawn r 30; lifetime [1,1.3] (≈ time to reach centre under 170 px/s²); `alignToVelocity:true` + `velocityRotationOffset:1.5708` (trace_02 streak is drawn vertical in texture, so +π/2 aligns it along velocity); startScale [0.04,0.09], shrink to 0.45, alpha ramp 0.15/0.85.
   • AMBIENT DEBRIS / SPARKLES: spawn radius as wide as the effect (195–210), speed 2–22, spread 360, no gravity; debris star_02 lifetime [2,3], rotationSpeed ±1, hold alpha 0.85; sparkles star_08 lifetime [0.22,0.5], twinkle scale 0.6→1.15→0.7.
   • LIGHTNING ARC (orb-*-arc): spark_05/spark_06 randomly rotated (rotationStart [0,6.283]) around the orb, spawn circle r 21–36 ≈ 0.11× orb glow px, speed [0,4]–[0,20], spread 360, lifetime 0.09–0.4 s, alpha pop [[0,0],[0.05,1],[0.25,0.7],[1,0]], white tint, add blend, startScale ≈ 0.9× orb glow scale.
   • alignToVelocity: rotation += atan2(vy,vx) + velocityRotationOffset, updated after integration so gravity curving shows immediately; below VELOCITY_EPSILON 0.001 px/s keeps last angle (vfx-core.js:~904-912). Use offset π/2 for textures drawn vertical, 0 for textures drawn along +x.
exportFacts: RUN: `node tools/vfx/export-assets.cjs` (export), `--check` (dry-run; exit 3 if images/vfx/assets or vfx/shipped-assets.json are out of date → CI-friendly), `--root <libraryPath>` (override). Library root otherwise resolved by tools/vfx/vfx-library-root.cjs: env VFX_ASSET_ROOT_EFFECTS_MATERIALS → VFX_ASSET_LIBRARY_ROOT → vfx/library.local.json (currently D:/MyGames/Effects-Materials; not versioned). Exit codes (export-assets.cjs:48-53): 0 ok, 1 export failed (state restored), 2 precondition (missing vfx/asset-index.json → run `node tools/vfx/asset-scanner.cjs` first), 3 out of date, 4 recovery incomplete (manual fix).
   
   WHAT IT VALIDATES (buildPlan :382-468, preflight :472-505): reads EVERY vfx/presets/*.json as a preset (readPresets :347-365 — so never drop non-preset JSON there; layouts live in vfx/layouts for this reason), each must pass VFXCore.validatePreset; collects every layer.assetId (collectAssetIds :367-378); each assetId MUST exist in vfx/asset-index.json (:425 "索引中沒有這個 assetId" → nothing written); relativePath must be portable (checkPortableRelativePath :122-139: no `\`, `..`, drive letters, `<>:"|?*`, trailing space/dot, Windows device names, and not the reserved marker name); index contentHash must be `sha256:<64hex>` and fileSize integer; actual file size and sha256 must match the index (else "請重跑 tools/vfx/asset-scanner.cjs"); file signature must match extension (.png/.jpg/.jpeg/.webp magic bytes, .svg starts with `<`, :94-100/:202-218); no case-insensitive destination collisions, no file-vs-dir path conflicts; no symlinks anywhere on source or destination paths.
   
   HOW IT WRITES (:553-700, :711-844): constants only — EXPORT_DIR_REL `images/vfx/assets`, SHIPPED_INDEX_REL `vfx/shipped-assets.json`, BASE_URL `images/vfx/assets` (:66-69), no CLI override (tests assert this). Builds a full staging tree `images/vfx/.vfx-export-staging-<pid>-<hex>` mirroring the library's relativePath (case preserved: "particle-pack/PNG (Black background)/flame_04.png"), validates staging hash-by-hash, renames live → `.vfx-export-backup-*`, staging → live, writes shipped index via exclusive tmp `.shipped-assets.json.tmp-*` + rename, then deletes backup. Whole tree replaced → stale assets vanish automatically; no-op when already up to date.
   
   OWNERSHIP MARKER: `images/vfx/assets/.vfx-export-root.json` must be byte-identical to markerContent() (:224-232): {"kind":"vfx-export-root","schemaVersion":1,"owner":"idle-rpg-vfx-exporter","repoRelativePath":"images/vfx/assets","note":"這個目錄由 tools/vfx/export-assets.cjs 完全管理…"}. checkMarkerFile (:235-263) rejects symlink/hardlink/extra fields/any drift; assertOwnedTree (:291-322) additionally requires managed parent dir + name pattern; otherwise the tool REFUSES to delete/replace (hint: delete the dir manually and re-run). Never hand-place files in images/vfx/assets (they'd be wiped, or block export). Note `images/vfx/thrust_lance.png` sits outside the managed dir and is untouched.
   
   vfx/shipped-assets.json (current): {schemaVersion 1, kind "vfx-shipped-assets", note, libraryId "effects-materials", baseUrl "images/vfx/assets", assetCount 20, assets:[{assetId, relativePath, package, contentHash, fileSize}]} — 7 light-masks-1.0 (circle_a, circle_a_noise, circle_b, circle_rings_a, cone_composed_c_noise, ring_a, transparent/circle_a), 12 particle-pack (flame_04, light_03, magic_03, spark_01, spark_05, spark_06, star_02, star_08, trace_02, twirl_01, twirl_02, transparent/smoke_04), 1 splat-pack (splat05). assetId is the lowercase slug ("particle-pack/png-black-background/flame_04.png"), relativePath the real path.
   
   PRODUCTION RESOLVER: shipped-assets.json has the same `assets[].{assetId, relativePath}` shape as asset-index.json, so `VFXCore.createIndexResolver(shipped, shipped.baseUrl)` (vfx-core.js:1098-1112) works directly — proven in tests/vfx-asset-export.test.cjs:316. It returns `baseUrl + '/' + relativePath` with each segment encodeURIComponent'd (e.g. `images/vfx/assets/particle-pack/PNG%20(Black%20background)/flame_04.png`), exposes `has(assetId)` and `resolve(assetId)` (throws on unknown). Runtime wiring: `VFXPixiBackend.createBackend({PIXI, container})` (js/vfx-pixi-backend.js; container gets sortableChildren=true; blend map normal/add/multiply/screen 1:1), `VFXCore.createRuntime({backend, resolver, budget})`, `runtime.registerPreset(presetJson)` (validates + resolves all assetIds up-front, fails fast), `runtime.play(id, {position, rotation, scale, seed, startTime})` → handle, `runtime.update(dtSeconds)` each frame, `stop/stopAll/timeOf/stats/destroy`, and (uncommitted WIP) `setTransform(handle, params)`. No game-side consumer of shipped-assets.json or VFXCore exists yet (grep of js/ finds only vfx-core.js and vfx-pixi-backend.js). Presets themselves are fetched from vfx/presets/<id>.json (not bundled). Workflow for a new preset needing new textures: 1) pick assetIds present in vfx/asset-index.json (else rescan), 2) save preset, 3) `node tools/vfx/export-assets.cjs`, 4) commit images/vfx/assets/** + vfx/shipped-assets.json + the preset.
editorFacts: SERVER (tools/vfx/editor-server.cjs): `node tools/vfx/editor-server.cjs [--port N]` (or tools/vfx/editor_server_window.bat); listens 127.0.0.1 only, port 28361 + up to 9 retries (:63-64); `/` → /tools/vfx/editor/index.html (:542); `/__whoami` returns "idle-rpg-vfx-editor <repoRoot>" to tell worktrees apart (:538-540). GET allowlist REPO_ALLOWLIST = ['/tools/vfx/editor/', '/tools/vfx/vfx-semantic-vocab.cjs', '/js/', '/vfx/'] (:61-62); any path segment starting with '.' → 403 (:562); `/asset-library/<libraryId>/<rel>` serves the machine-local library root (:544-555). serveFile 404s unless the target is a regular file (:157) → **there is no directory listing; GET /vfx/presets/ returns 404**. Cache: editor files no-store, others no-cache (:151-153). WRITE routes (only PUT, :509-521): `PUT /vfx/presets/<id>.json` and `PUT /vfx/layouts/<id>.json`; id taken from the raw undecoded path and must pass tools/vfx/editor/preset-id-policy.js (`^[a-z0-9][a-z0-9-]*$`, ≤64 chars, not con/prn/aux/nul/comN/lptN); body ≤1 MB and Content-Type application/json; connection, Host and Origin must be loopback (:430-452); preset body is parsed → VFXCore.validatePreset → `preset.id` must equal the filename id (:347-352) → VFXCore.serialisePreset canonical text → exclusive temp `.vfx-save-tmp-*` + fsync + rename (:227-329). Layouts validated by tools/vfx/editor/layout-schema.js (presetId must match filename, :380-385).
   
   EDITOR (tools/vfx/editor/editor.js): `?preset=<id>` selects the boot preset via presetIdFromQuery (:23-29; falls back to DEFAULT_PRESET_ID 'demo-basic' if absent/invalid); presetUrl(id) = '/vfx/presets/'+id+'.json' (:31). boot() (:2420-2431) fetches /vfx/asset-index.json, /vfx/asset-semantics.json, the one preset, and /vfx/layouts/<id>.json (404 → empty layout, :1300-1302). Editor resolver = VFXCore.createIndexResolver(index, '/asset-library/'+libraryId) (:2465-2466) — identical Core, so what you see is what runtime plays. state.sourcePresetId = boot id; savePreset (:2183-2247) PUTs to presetUrl(state.preset.id) and refuses if preset.id ≠ sourcePresetId ("would overwrite another file"). "載入 Preset" button (index.html:20,23) opens the OS file picker → loadPresetFromFile (:2268-2300) imports local JSON (sourcePresetId=null, so Save writes <preset.id>.json — the path to bring an externally authored preset into the repo). "下載複本" downloads JSON. Undo/redo history is cleared on preset switch (:2262-2266).
   
   VERDICT: the editor can only OPEN ONE PRESET BY ID (URL query or file import); it cannot list vfx/presets. To add a preset dropdown: (1) server — add a GET route in createServer (editor-server.cjs:504-569, before the allowlist check) e.g. `/__presets` that `fs.readdirSync(<repo>/vfx/presets)` filters `/\.json$/`, strips the extension, keeps only ids passing presetIdPolicy.isWritablePresetId (this also excludes `.vfx-save-tmp-*`), and returns a JSON array (do NOT create a static index file inside vfx/presets — export-assets reads every *.json there as a preset; vfx/ root would be fine); (2) editor — add `<select id="sel-preset">` to the topbar in index.html, populate it in boot() from that route, and on change either navigate `location.search = '?preset=' + id` (simplest: reuses boot, resets history/dirty/layout/selection) or refactor the boot Promise into `loadPresetById(id)` that resets state.preset/layout/savedText/savedLayoutText/sourcePresetId/selection/gizmo and calls history.clear(), guarding on the dirty flag first; (3) optionally add a "Save As" that changes preset.id + sourcePresetId together. No other file needs changing; tests in tests/vfx-editor-save.test.cjs cover the PUT path only.
durationLoop: Preset-level: `duration` (s, >0, ≤60 — HARD_LIMITS.maxDuration vfx-core.js:50) and `loop` (bool, default false). Persistent fields use loop:true with duration = one visual period: black-hole 3 s (1 outer revolution), fire-tornado 2.4 s (swirl 1 rev, ring re-flash), lightning-orb-field/-b 4 s (2 flicker cycles). One-shots use loop:false: demo-basic 1.6 s. Per-layer `delay` (s) shifts a layer's start and `duration` overrides the preset duration for that layer (layerLife vfx-core.js:704-713: progress = (time−delay)/duration; layer inactive outside → node released) — demo-basic: ring duration 1.2, smoke delay 0.1 + duration 1.5, sparks burst at t=0. Effects end only when `!loop && time ≥ duration` AND no live particles (vfx-core.js:980-982), so particle lifetimes may exceed duration safely. On loop wrap `time %= duration` and every layer's burstDone resets (vfx-core.js:971-974) → bursts re-fire each cycle, rate emitters run continuously, sprite curves must be seamless (start value == end value) or accept a visible snap (fire ground-ring intentionally snaps). `play(id,{startTime})` scrubs sprites exactly; particles start with no history. Editor "Loop" checkbox writes preset.loop (editor.js:2598-2601). Curves are in normalized life t (0..1), so changing `duration` rescales all timing — set duration first, then author curves. Rotation-per-loop figures (2π·N) are tied to duration: 6.28318 over 3 s = 0.33 rev/s.
zIndexBlendTint: zIndex: any finite number; Pixi container has sortableChildren=true (vfx-pixi-backend.js:52) so higher draws on top within the effect; equal zIndex → node creation order (unstable when layers reorder — keep unique). Conventions: black-hole 0..11 back→front (outer glow 0, ring 1, swirls 2-3, inflow particles 4-7, debris 8, inner accretion 9, opaque black event-horizon 10, photon ring 11); fire-tornado 0..9 (scorch 0, ring 1, base glow 2, funnel 3, swirls 4-5, flame 6, embers 7, sparks 8, smoke 9 on top); lightning fields use decades for insertion room (field glow 0, orb glow 10, body 20, core 21, arc 22, sparkles 31). Layer array order still matters for RNG seeds and equal-z ties.
   
   blendMode ∈ normal|add|multiply|screen (1:1 to Pixi v8 strings). Rule: every "PNG (Black background)" and light-masks/default asset MUST use `add` (black → transparent, colours sum); transparent-variant assets (smoke_04, splat05, light-masks/transparent/circle_a) use `normal` for occluding/darkening elements: smoke tint #5c5148 (alpha 0.4 peak) / #8a8a8a, scorch splat #3b2a20 alpha 0.5, event-horizon disc #000000 alpha 1 (the only opaque occluder). multiply/screen unused so far. Editor canvas is opaque (#101014) precisely so additive layers don't produce black squares (editor.js:2471-2472).
   
   Tint: `#rrggbb` only (COLOR_RE vfx-core.js:121), whole layer, multiplied onto the greyscale masks. Palettes used — FIRE: #ff4a08 (funnel body), #ff7a24 / #ff7a1e (ring / flame core), #ff8a2b (base glow), #ff7020 & #ffa03a (swirls), #ffc061 (embers), #fff0b8 (sparks, near white), #ffb066 / #ffd27f (demo ring / sparks). BLACK HOLE: #5a3f9e purple outer glow (alpha 0.35), #ff9a3c / #ffb066 / #ffbe6a / #ffd9a0 orange accretion & inflow, #9fd8ff blue inner swirl, #cfd9ff debris, #ffe6b8 photon ring. LIGHTNING: #4a7fea field glow, #6fb8ff / #7cb5ff / #5aa8f5 orb glow, #7094ff / #b7e2ff / #c8ebff / #d4f0ff orb body, #ffffff cores, arcs, sparkles. Alpha layering: outer glows 0.32–0.55, bodies 0.84–1.0, cores 0.46–0.7 (white, small), swirls 0.32–0.4, decals 0.5; particle layers leave alpha at 1 and shape it with alphaOverLife. Hot core = smaller, whiter, higher-z; halo = larger, saturated, lower-alpha, lower-z.
